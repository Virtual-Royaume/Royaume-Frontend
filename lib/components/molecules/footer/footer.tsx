import clsx from "clsx";
import { ReactElement } from "react";

export const Footer = (): ReactElement => {
  return (
    <footer className="relative z-30 mt-28 h-24 bg-background-card border-t-4 border-t-purple">

    </footer>
  );
};